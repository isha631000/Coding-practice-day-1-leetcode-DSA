def midPoint(head) :
    slow=head
    fast=head
    if head == None:
        return head
    while (fast.next is not None and fast.next.next is not None):
        slow=slow.next
        fast=fast.next.next
    return slow
